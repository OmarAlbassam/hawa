__pycache__/
*.pyc
.venv/
venv/
.env
.env.local
.pytest_cache/
.ipynb_checkpoints/
*.ipynb_meta

# Cached LLM responses (sqlite) and embedding artifacts
cache.db
cache.db-*
cache.db.bak
*.npz

# Result artifacts (parquet, plots)
results/*.parquet
results/*.png
results/*.csv
!results/.gitkeep

# Streamlit
.streamlit/

# Local model downloads (sentence-transformers caches in ~/.cache, but keep
# this just in case anyone configures HF_HOME inside the app dir)
hf_models/
