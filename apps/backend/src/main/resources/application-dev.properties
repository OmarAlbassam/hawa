# Dev profile — local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/hawa
spring.datasource.username=hawa
spring.datasource.password=hawa

# Show SQL for debugging
spring.jpa.show-sql=true
